import { useMemo, useState } from 'react'
import { Layout } from '../components/Layout'
import { RecipeCard } from '../components/RecipeCard'
import { filterRecipes } from '../data/recipeFilters'
export function RecipesPage() { const [query, setQuery] = useState(''); const [prep, setPrep] = useState(0); const [cook, setCook] = useState(0); const filtered = useMemo(() => filterRecipes(query, prep, cook), [query, prep, cook]); return <Layout><section className="recipes-page shell" aria-labelledby="recipes-title"><header><h1 id="recipes-title">Explore our simple, healthy recipes</h1><p>Discover eight quick, whole-food dishes that fit real-life schedules and taste amazing. Search by name or ingredient, or let something delicious catch your eye.</p></header><div className="filters"><label>Max Prep Time<select value={prep} onChange={(event) => setPrep(Number(event.target.value))}><option value="0">Any time</option><option value="5">5 minutes</option><option value="10">10 minutes</option></select></label><label>Max Cook Time<select value={cook} onChange={(event) => setCook(Number(event.target.value))}><option value="0">Any time</option><option value="5">5 minutes</option><option value="10">10 minutes</option><option value="15">15 minutes</option><option value="20">20 minutes</option></select></label><label className="search"><span className="sr-only">Search by name or ingredient</span><img src="/assets/images/icon-search.svg" alt="" /><input type="search" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by name or ingredient…" /></label></div><p className="result-count" role="status">{filtered.length} {filtered.length === 1 ? 'recipe' : 'recipes'} found</p>{filtered.length ? <div className="recipe-grid">{filtered.map((recipe) => <RecipeCard key={recipe.id} recipe={recipe} />)}</div> : <div className="no-results"><h2>No recipes found</h2><p>Try a different ingredient or widen the time filters.</p></div>}</section></Layout> }
